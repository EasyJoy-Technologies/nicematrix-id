{
  "task": "Fix Sign-in & account save error (entity_not_exists_with_names: name, avatar) in Logto 1.41 + verify username-dot override still effective",
  "updated": "2026-07-15",
  "issue2_username_dot": {
    "status": "VERIFIED GREEN — no change needed",
    "finding": "1.41 added username-policy.ts; dot only passes via validateUsernameHardFloor()->usernameRegEx. Deployed image regex.js = /^[A-Z_a-z](?:[\\w.]*\\w)?$/ and username-policy.js imports it. Dots still work."
  },
  "issue1_profilefields_fix": {
    "status": "DONE — committed (8e18cd6), built (image 955ff9c7fab5/238b2fd04473), staging+prod-1 deployed, 6/6 smoke green both, DB restored, logs clean",
    "root_cause": "1.41 alteration seeds admin account_centers.profile_fields=[{name},{avatar}]; validateProfileFieldsList validates all names vs custom_profile_fields catalog with no built-in exemption; name/avatar are column-backed, never rows -> save always 400.",
    "override_file": "logto-custom/overrides/packages/core/src/libraries/custom-profile-fields/index.ts",
    "fix": "Exempt only name+avatar (nameAndAvatarGuard.keyof()) from missing-name check; keep duplicate check on full list; skip catalog query when namesToVerify empty (avoid `in ()`). Other built-ins (birthdate/gender/nickname/address) stay validated (real catalog rows here).",
    "docs": "patches.md item 7; account-center-profile.md server-side overrides section",
    "no_db_migration": true
  },
  "deploy_plan": "Build image (deploy/docker-compose.yml) -> tag staging-backup + feature tag -> staging up -d -> smoke (save Sign-in&account 200 via mgmt API; unknown custom name still 400; duplicate still 400; OIDC/console/account 200; log clean) -> save image tar -> scp prod-1 (md5 verify) -> tag prod-backup -> load+recreate -> same smoke -> changelog + MEMORY.md.",
  "rollback": "staging: tag staging-backup->latest + up -d. prod-1: tag prod-backup-<date>->latest + up -d. No DB change."
}
